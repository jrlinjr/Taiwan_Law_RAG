# LangChain 核心
langchain-core>=1.4.0

# LangChain 整合
langchain-ollama>=1.1.0
langchain-qdrant>=1.1.0

# 向量資料庫
qdrant-client>=1.7.3

# PDF 解析
pypdf>=4.0.0

# Web UI
gradio>=6.0.0

# 配置管理
python-dotenv>=1.0.0

# 工具類
requests>=2.31.0

# 註:Embedding 由遠端 Ollama 伺服器計算(EMBEDDING_PROVIDER=ollama)。
# 若要改用本機 HuggingFace 模型(EMBEDDING_PROVIDER=huggingface),需另外安裝:
#   pip install langchain-huggingface sentence-transformers torch accelerate
